/** @deprecated Import from `../../telegram/parseSocksHttpProxy` — re-export for existing paths. */
export {
  coerceSocksHttpImportFields,
  parseSocksHttpProxyLine,
  type ParsedSocksHttpProxy,
} from '../../telegram/parseSocksHttpProxy';
